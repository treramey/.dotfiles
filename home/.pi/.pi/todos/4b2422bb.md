{
  "id": "4b2422bb",
  "title": "Patch pi extensions for v0.64.0 API compatibility",
  "tags": [
    "pi",
    "extensions",
    "compat"
  ],
  "status": "closed",
  "created_at": "2026-03-30T12:28:01.850Z"
}

Applied compatibility fixes:

- Updated `home/.pi/agent/extensions/answer.ts` to use `modelRegistry.getApiKeyAndHeaders()` instead of removed `getApiKey()`, and forward both `apiKey` and `headers` into `complete()`.
- Updated `home/.pi/agent/extensions/web-tools/render.ts` to use the current keybinding id for tool expansion hints.
- Updated `home/.pi/package.json` dependencies to `^0.64.0` for `@mariozechner/pi-ai`, `@mariozechner/pi-coding-agent`, and `@mariozechner/pi-tui`.
- Ran `bun install` in `home/.pi`, which updated local installed versions to `0.64.0` and refreshed `bun.lock`.
- Verified targeted TypeScript compatibility with:
  `bunx tsc --noEmit --module esnext --moduleResolution bundler --target es2022 --allowImportingTsExtensions --skipLibCheck agent/extensions/answer.ts agent/extensions/web-tools/render.ts`
- Verified no remaining `getApiKey(` or `expandTools` usages in local extensions.
