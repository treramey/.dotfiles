{
  "name": ".pi",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "agent/extensions/*"
  ],
  "scripts": {
    "check": "npm run check:safety && npm run check --workspace=pi-cfpaste-extension && npm run check --workspace=pi-save-md-extension && npm run check --workspace=pi-worktrees-extension && npm run check --workspace=pi-handoff-extension",
    "check:safety": "tsc --project agent/extensions/tsconfig.safety.json --pretty false && node --import tsx --test agent/extensions/tests/*.test.ts",
    "test:cfpaste": "npm run test --workspace=pi-cfpaste-extension",
    "test:save-md": "npm run test --workspace=pi-save-md-extension",
    "test:worker-configuration-guard": "node --import tsx --test agent/extensions/tests/worker-configuration-guard.test.ts",
    "test:codex-fast-mode": "node --import tsx --test tests/codex-fast-mode.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.84.3",
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@earendil-works/pi-tui": "^0.84.3",
    "typebox": "^1.1.38"
  }
}
